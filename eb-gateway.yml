zuul:
  sensitiveHeaders: Cookie,Set-Cookie,Authorization

management:
  endpoints:
    web:
      exposure:
        include: "*"

zuul:
  ignoredServices: "eb-config-server, eb-discovery, eb-security"

server:
  port: 5555
