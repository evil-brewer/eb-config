spring:
  boot:
    admin:
      client:
        url: http://localhost:8085

server:
  port: 5555

management:
  endpoints:
    web:
      exposure:
        include: "*"

endpoints:
  security:
    name: eb-security
    oauth-url: auth/oauth/token
