spring:
  boot:
    admin:
      client:
        url: http://localhost:8085

zuul:
  sensitiveHeaders: Cookie,Set-Cookie

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 5555

security:
  oauth2:
    resource:
      userInfoUri: http://localhost:8901/auth/user
