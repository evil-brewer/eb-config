spring:
  data:
    mongodb:
      uri: mongodb://customer:customer@localhost:27017/dbCustomer
  boot:
    admin:
      client:
        url: http://localhost:8085

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 8081
